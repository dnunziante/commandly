import { AppShell } from "@/components/app-shell"; import { PageHeader } from "@/components/page-header"; import { BarChart3, MessageSquare, Target, Users } from "lucide-react";
export default function Analytics(){const topics=[["Product comparisons",88],["Price objections",72],["Follow-up messages",61],["Financing questions",43]];return <AppShell title="Analytics"><PageHeader eyebrow="Workspace insights" title="See where the team needs support" description="Sample activity shows how leaders can identify coaching and content opportunities." action={<select className="input" style={{width:160}}><option>Last 30 days</option><option>Last 7 days</option></select>}/><div className="grid grid-4"><div className="card"><Users/><div className="metric">18</div><p>Active reps</p></div><div className="card"><MessageSquare/><div className="metric">412</div><p>Assistant questions</p></div><div className="card"><Target/><div className="metric">76%</div><p>Training completion</p></div><div className="card"><BarChart3/><div className="metric">4.6</div><p>Average confidence</p></div></div><div className="grid grid-2" style={{marginTop:18}}><div className="card"><h2>Most requested help</h2>{topics.map(([name,val])=><div key={name as string} style={{marginTop:18}}><div className="metric-row"><span style={{fontSize:13}}>{name}</span><strong>{val}%</strong></div><div className="progress" style={{marginTop:6}}><span style={{width:`${val}%`}}/></div></div>)}</div><div className="card"><h2>Manager insight</h2><div className="callout"><strong>Price confidence is improving</strong><p style={{marginBottom:0}}>Reps who completed the value-over-price lesson used the objection assistant 31% less often in the following week.</p></div><p style={{fontSize:12}}>Prototype analytics use temporary sample data.</p></div></div></AppShell>}

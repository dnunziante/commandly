import { AlertTriangle, CheckCircle2, ClipboardList, Scale, TimerReset } from "lucide-react"; import { redirect } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { ExecutiveDecisionLog } from "@/components/executive-decision-log"; import { PageHeader } from "@/components/page-header"; import { ReportingPeriodSelector } from "@/components/reporting-period-selector"; import { getExecutiveDecisions } from "@/lib/executive/decision-repository"; import { getExecutiveWorkspace } from "@/lib/executive/repository";
export default async function DecisionsPage({ searchParams }: { searchParams: Promise<{ period?: string }> }) { const { period } = await searchParams; const [data, workspace] = await Promise.all([getExecutiveDecisions(period), getExecutiveWorkspace(period)]); if (!data.canView) redirect("/dashboard"); const cards = [["Recorded decisions", data.summary.total, ClipboardList], ["Open decisions", data.summary.open, Scale], ["Validated outcomes", data.summary.validated, CheckCircle2], ["Due for review", data.summary.dueForReview, TimerReset]] as const; return <AppShell title="Leadership Decisions"><PageHeader eyebrow="Executive Advisor" title="Remember why a decision was made—and whether it worked" description="Connect leadership decisions to priorities, ownership, expected outcomes, and measured results."/>{data.error && <p className="form-error"><AlertTriangle size={15}/>{data.error}</p>}<ReportingPeriodSelector action="/executive/decisions" periods={data.availablePeriods} selected={data.reportingPeriod} label="Decision period"/><section className="grid grid-4 executive-metrics" aria-label="Decision summary">{cards.map(([label,value,Icon]) => <article className="card" key={label}><div className="metric-row"><span>{label}</span><span className="metric-icon"><Icon size={18}/></span></div><div className="metric">{value}</div></article>)}</section><ExecutiveDecisionLog initialDecisions={data.decisions} period={data.reportingPeriod} priorities={workspace.priorities.map((p) => ({ id: p.id, title: p.title }))} persistence={data.persistence}/></AppShell>; }

"use client"; import { useState } from "react"; import { AppShell } from "@/components/app-shell"; import { PageHeader } from "@/components/page-header"; import { ArrowRight, CheckCircle2, Play } from "lucide-react";
const scenarios=[{title:"The price comparison",level:"Intermediate",copy:"A shopper found a lower advertised price and wants to know why BGC costs more."},{title:"I need to think about it",level:"Beginner",copy:"Uncover the uncertainty without adding pressure."},{title:"Premium cart skeptic",level:"Advanced",copy:"Connect Nexus features to a customer focused only on basic transportation."}];
export default function RolePlay(){const [active,setActive]=useState<number|null>(null);return <AppShell title="Role Play"><PageHeader eyebrow="Practice studio" title="Build confidence before the customer arrives" description="Work through realistic dealership conversations and receive simulated coaching."/>{active===null?<div className="grid grid-3">{scenarios.map((s,i)=><div className="card scenario" key={s.title}><span className="number">0{i+1}</span><span className="badge blue">{s.level}</span><h2 style={{marginTop:18}}>{s.title}</h2><p>{s.copy}</p><button className="btn btn-primary" onClick={()=>setActive(i)}><Play size={15}/> Start scenario</button></div>)}</div>:<div className="grid grid-2"><div className="card"><span className="badge amber">Live practice</span><h2 style={{marginTop:15}}>{scenarios[active].title}</h2><div className="message ai" style={{maxWidth:"100%",marginTop:20}}>I like the cart, but another dealer has one online for less. Why should I pay more here?</div><textarea className="input" rows={7} placeholder="Type how you would respond…" style={{marginTop:18}}/><button className="btn btn-primary" style={{marginTop:12}}><ArrowRight size={16}/> Submit response</button></div><div className="card"><CheckCircle2 color="#16825d"/><h2 style={{marginTop:12}}>Coaching focus</h2><p>Validate the concern, ask what the comparison includes, and relate BGC’s included value to the shopper’s priorities.</p><button className="btn btn-ghost" onClick={()=>setActive(null)}>Choose another scenario</button></div></div>}</AppShell>}

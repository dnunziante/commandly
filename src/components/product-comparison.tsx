"use client";

import Link from "next/link";
import { Check, GitCompareArrows, ImageIcon, Plus, RotateCcw, X } from "lucide-react";
import { useMemo, useState } from "react";
import type { ProductDTO } from "@/lib/products/types";

const fields = [{ label:"Starting price", value:(p:ProductDTO)=>`$${p.price.toLocaleString()}` },{label:"Passenger capacity",value:(p:ProductDTO)=>p.seats},{label:"Frame",value:(p:ProductDTO)=>p.range},{label:"Powertrain",value:(p:ProductDTO)=>p.powertrain},{label:"Description",value:(p:ProductDTO)=>p.description}];
const modelOrder=["Pulse","Beyond","Nexus","SIVO"];
const isComparisonAccessory=(product:ProductDTO)=>/grab bar|side steps?/i.test(product.name);
const display=(value:string)=>value.trim()||"Not added";
const different=(values:string[])=>new Set(values.map((value)=>display(value).toLowerCase())).size>1;

export function ProductComparison({ products }: { products: ProductDTO[] }) {
  const comparisonProducts=useMemo(()=>products.filter((product)=>!isComparisonAccessory(product)&&modelOrder.some((model)=>product.name.toLowerCase().includes(model.toLowerCase()))).sort((a,b)=>{const group=modelOrder.findIndex((model)=>a.name.toLowerCase().includes(model.toLowerCase()))-modelOrder.findIndex((model)=>b.name.toLowerCase().includes(model.toLowerCase()));return group||a.price-b.price||a.name.localeCompare(b.name);}),[products]);
  const initialIds=comparisonProducts.slice(0,2).map((product)=>product.id);
  const [selectedIds,setSelectedIds]=useState<string[]>(initialIds);
  const selectedProducts=useMemo(()=>selectedIds.map((id)=>comparisonProducts.find((product)=>product.id===id)).filter((product):product is ProductDTO=>Boolean(product)),[comparisonProducts,selectedIds]);
  const selectProduct=(slot:number,id:string)=>setSelectedIds((current)=>current.map((item,index)=>index===slot?id:item));
  const addProduct=()=>{const next=comparisonProducts.find((product)=>!selectedIds.includes(product.id));if(next&&selectedIds.length<3)setSelectedIds((current)=>[...current,next.id]);};
  return <div className="comparison-workspace"><section className="card comparison-picker"><div className="comparison-picker-heading"><div><h2>Choose products</h2><p>Compare Pulse, Beyond, Nexus, and SIVO configurations. Options are grouped by model and priced low to high.</p></div><span className="badge blue"><GitCompareArrows size={14}/>{selectedProducts.length} selected</span></div><div className="comparison-selectors">{selectedIds.map((id,index)=><div className="comparison-selector" key={index}><label className="label" htmlFor={`comparison-product-${index}`}>Product {index+1}</label><div><select className="input" id={`comparison-product-${index}`} value={id} onChange={(event)=>selectProduct(index,event.target.value)}>{modelOrder.map((model)=>{const grouped=comparisonProducts.filter((product)=>product.name.toLowerCase().includes(model.toLowerCase()));return grouped.length?<optgroup key={model} label={model}>{grouped.map((product)=><option value={product.id} disabled={selectedIds.includes(product.id)&&product.id!==id} key={product.id}>{product.name} · {product.model||"Standard"} · ${product.price.toLocaleString()}</option>)}</optgroup>:null;})}</select>{selectedIds.length===3&&<button className="icon-btn comparison-remove" type="button" onClick={()=>setSelectedIds((current)=>current.filter((_,itemIndex)=>itemIndex!==index))} aria-label={`Remove product ${index+1}`}><X size={17}/></button>}</div></div>)}{selectedIds.length<3&&comparisonProducts.length>selectedIds.length&&<button className="comparison-add" type="button" onClick={addProduct}><Plus size={20}/><span><strong>Add a third product</strong><small>Optional comparison</small></span></button>}</div><button className="text-button" type="button" onClick={()=>setSelectedIds(initialIds)}><RotateCcw size={14}/> Reset comparison</button></section><div className="comparison-table-wrap"><div className="comparison-detail-grid" style={{gridTemplateColumns:`minmax(145px,.55fr) repeat(${selectedProducts.length}, minmax(230px,1fr))`}}><div className="comparison-corner"><span>Product details</span></div>{selectedProducts.map((product)=><article className="comparison-product-head" key={product.id}>{product.imageUrl?<div className="comparison-product-image" style={{backgroundImage:`url(${product.imageUrl})`}} role="img" aria-label={`${product.name} product image`}/>:<div className={`comparison-product-image placeholder ${product.color}`}><ImageIcon size={28}/><span>Image not added</span></div>}<span className="badge blue">{product.status}</span><h2>{product.name}</h2><p>{product.model||"Standard configuration"}</p></article>)}{fields.map((field)=>{const values=selectedProducts.map(field.value);return <div className="comparison-row" key={field.label} style={{gridColumn:"1 / -1",gridTemplateColumns:`minmax(145px,.55fr) repeat(${selectedProducts.length}, minmax(230px,1fr))`}}><div className="comparison-label">{field.label}{different(values)&&<small>Different</small>}</div>{values.map((value,index)=><div className={different(values)?"comparison-value is-different":"comparison-value"} key={`${field.label}-${selectedProducts[index].id}`}>{display(value)}</div>)}</div>;})}<div className="comparison-row" style={{gridColumn:"1 / -1",gridTemplateColumns:`minmax(145px,.55fr) repeat(${selectedProducts.length}, minmax(230px,1fr))`}}><div className="comparison-label">Saved highlights</div>{selectedProducts.map((product)=><div className="comparison-value comparison-highlights" key={product.id}>{product.highlights.length?<ul>{product.highlights.map((highlight)=><li key={highlight}><Check size={15}/><span>{highlight}</span></li>)}</ul>:<span className="comparison-missing">Not added</span>}</div>)}</div><div className="comparison-row comparison-actions-row" style={{gridColumn:"1 / -1",gridTemplateColumns:`minmax(145px,.55fr) repeat(${selectedProducts.length}, minmax(230px,1fr))`}}><div className="comparison-label">More information</div>{selectedProducts.map((product)=><div className="comparison-value" key={product.id}><Link className="btn btn-secondary" href={`/products/${product.slug}`}>View product guide</Link></div>)}</div></div></div></div>;
}
